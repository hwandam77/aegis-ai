# TDD 통합 측정 지표 (KPIs)

## 📊 지표 개요

이 문서는 TDD 통합의 성공을 측정하고 추적하기 위한 핵심 성과 지표(KPI)를 정의합니다.

**관리 주체**: Claude Code (총괄 책임 PM)

---

## 🎯 Claude Code PM 대시보드

### 실시간 PM 대시보드

```
┌─────────────────────────────────────────────────────────────┐
│  🎯 Claude Code PM Dashboard - TDD Integration              │
├─────────────────────────────────────────────────────────────┤
│  📅 Project: codex-qwen-gemini-mcp                          │
│  📊 Current Phase: Phase 1 (Infrastructure)                 │
│  🎯 Overall Progress: ████░░░░░░░░░░░░░░░░ 20%            │
│  ⏱️  Elapsed: 3 days / Target: 10 weeks                     │
├─────────────────────────────────────────────────────────────┤
│  🤖 AI Team Status                                          │
│     Gemini  (Speculator)  : ████████████░░░░░░░░ Idle      │
│     Qwen    (Technician)  : ████████████████████ Working   │
│     Codex   (Refactorer)  : ████████████░░░░░░░░ Idle      │
├─────────────────────────────────────────────────────────────┤
│  📈 Key Metrics (Updated: 2025-11-22 09:00)                │
│     Coverage     : 23% (↑ 8% this week)                    │
│     Tests        : 12  (↑ 9 this week)                     │
│     Pass Rate    : 100%                                    │
│     TDD Adoption : 75% (PRs with tests)                    │
├─────────────────────────────────────────────────────────────┤
│  ⚠️  Active Risks                                           │
│     🟢 No critical risks                                    │
│     🟡 R-003: Mocking complexity (Medium) - Monitoring     │
├─────────────────────────────────────────────────────────────┤
│  🎯 Next Milestones                                         │
│     [██████░░░░] Phase 1 Complete (Week 2) - 60%           │
│     [░░░░░░░░░░] Phase 2 Start (Week 3) - 0%              │
├─────────────────────────────────────────────────────────────┤
│  💡 PM Actions Today                                        │
│     ✅ Approved Task 1.2 completion                         │
│     🔍 Monitoring Qwen test generation                      │
│     📊 Preparing weekly report                              │
└─────────────────────────────────────────────────────────────┘

Commands: 'status' | 'risk' | 'report' | 'approve' | 'delegate'
```

### PM 명령어 시스템

사용자가 PM에게 요청할 수 있는 명령:

```bash
# 현황 보고
Claude, status

# 리스크 분석
Claude, risk analysis

# 주간 리포트
Claude, weekly report

# Phase 승인
Claude, approve Phase 1

# AI 팀 작업 할당
Claude, delegate handlerLoader test to AI team
```

---

## 🎯 핵심 목표 지표

### 1. 코드 커버리지 (Code Coverage)

#### 정의
테스트로 실행되는 코드의 비율

#### 측정 방법
```bash
npm run test:coverage
```

#### 목표 값
| 모듈 | 초기 | 단기 (2개월) | 중기 (6개월) | 장기 (1년) |
|------|------|-------------|-------------|-----------|
| **core/** | 0% | 80% | 90% | 95% |
| **services/** | 0% | 70% | 80% | 85% |
| **handlers/** | 0% | 60% | 70% | 80% |
| **전체** | 0% | 70% | 80% | 90% |

#### 측정 주기
- **일일**: CI/CD 파이프라인에서 자동 측정
- **주간**: 주간 리포트에 포함
- **월간**: 월간 리뷰에서 트렌드 분석

#### 시각화
```bash
npm run dashboard
```

---

### 2. 테스트 수 (Test Count)

#### 정의
작성된 테스트 케이스의 총 개수

#### 측정 방법
```bash
npm test -- --listTests | wc -l
```

#### 목표 값
| Phase | 목표 테스트 수 | 누적 |
|-------|--------------|------|
| Phase 1 | 3 (기존) | 3 |
| Phase 2 | +50 | 53 |
| Phase 3 | +40 | 93 |
| Phase 4 | +30 | 123 |
| Phase 5 | +20 | 143 |
| Phase 6 | +10/월 | 200+ (1년 후) |

#### 분류별 목표
- **Unit Tests**: 120+
- **Integration Tests**: 50+
- **E2E Tests**: 30+

---

### 3. 테스트 통과율 (Test Pass Rate)

#### 정의
전체 테스트 중 통과하는 테스트의 비율

#### 측정 방법
```
Pass Rate = (Passing Tests / Total Tests) × 100
```

#### 목표 값
- **최소 목표**: 95%
- **이상적 목표**: 100%

#### 경고 임계값
- 🟢 **Green Zone**: 95% 이상
- 🟡 **Yellow Zone**: 90-95%
- 🔴 **Red Zone**: 90% 미만 (즉시 조치 필요)

---

### 4. 테스트 실행 시간 (Test Execution Time)

#### 정의
전체 테스트 스위트 실행에 걸리는 시간

#### 목표 값
- **Unit Tests**: < 30초
- **Integration Tests**: < 2분
- **E2E Tests**: < 5분
- **전체**: < 5분

#### 최적화 전략
- 병렬 테스트 실행
- Mock/Stub 활용
- 느린 테스트 식별 및 개선

---

## 📈 품질 지표

### 5. 버그 발견율 (Bug Detection Rate)

#### 정의
테스트를 통해 사전 발견된 버그 수 / 전체 버그 수

#### 측정 방법
```
Detection Rate = (Bugs Found by Tests / Total Bugs) × 100
```

#### 목표 값
| 기간 | 목표 |
|------|------|
| 2개월 | 50% |
| 6개월 | 70% |
| 1년 | 90% |

#### 분류
- **Pre-Production Bugs**: 테스트로 발견된 버그
- **Production Bugs**: 프로덕션에서 발견된 버그

---

### 6. 회귀 버그 방지율 (Regression Prevention Rate)

#### 정의
회귀 테스트로 방지된 버그의 비율

#### 측정 방법
- 버그 발생 시 회귀 테스트 작성 여부 추적
- 동일 버그 재발생 모니터링

#### 목표 값
- **회귀 테스트 작성율**: 100%
- **버그 재발율**: < 5%

---

### 7. 코드 품질 점수 (Code Quality Score)

#### 정의
정적 분석 도구(ESLint, SonarQube 등)의 품질 점수

#### 측정 항목
- **복잡도 (Complexity)**: 함수당 평균 복잡도
- **중복 코드 (Duplication)**: 중복 코드 비율
- **기술 부채 (Technical Debt)**: 추정 수정 시간

#### 목표 값
- **복잡도**: < 10 (McCabe Complexity)
- **중복 코드**: < 3%
- **기술 부채**: < 1일 (전체)

---

## 🚀 개발 효율성 지표

### 8. 개발 속도 (Development Velocity)

#### 정의
TDD 도입 전후 기능 개발 속도 비교

#### 측정 방법
- **Story Points / Sprint**
- **Features Delivered / Month**

#### 기대 효과
| 기간 | 예상 변화 |
|------|----------|
| 초기 (1-2개월) | -20% (학습 곡선) |
| 중기 (3-6개월) | +10% (안정성 증가) |
| 장기 (6개월+) | +30% (디버깅 시간 감소) |

---

### 9. 디버깅 시간 (Debugging Time)

#### 정의
버그 수정에 소요되는 평균 시간

#### 측정 방법
- JIRA/GitHub Issue 해결 시간 추적
- Bug Fix 커밋의 시간 분석

#### 목표 값
| 기간 | 평균 디버깅 시간 | 감소율 |
|------|-----------------|--------|
| 현재 | 4시간 | - |
| 2개월 | 3시간 | -25% |
| 6개월 | 2시간 | -50% |
| 1년 | 1시간 | -75% |

---

### 10. 리팩토링 빈도 (Refactoring Frequency)

#### 정의
안전하게 리팩토링이 수행된 횟수

#### 측정 방법
- "refactor:" 커밋 수
- 리팩토링 후 테스트 통과율

#### 목표 값
- **주간 리팩토링 커밋**: 5+ 회
- **리팩토링 성공률**: 100% (테스트 통과)

---

## 👥 팀 참여 지표

### 11. TDD 채택률 (TDD Adoption Rate)

#### 정의
TDD 사이클을 따르는 PR의 비율

#### 측정 방법
```
Adoption Rate = (PRs with TDD / Total PRs) × 100
```

#### 목표 값
| 기간 | 목표 |
|------|------|
| 2개월 | 50% |
| 6개월 | 80% |
| 1년 | 100% |

#### 검증 기준
PR이 다음을 포함하는지 확인:
- [ ] 테스트 코드
- [ ] TDD 체크리스트 완료
- [ ] 커버리지 임계값 충족

---

### 12. 코드 리뷰 품질 (Code Review Quality)

#### 정의
코드 리뷰에서 테스트 관련 피드백의 비율

#### 측정 방법
- 리뷰 코멘트 분석
- 테스트 관련 논의 비율

#### 목표 값
- **테스트 관련 리뷰 코멘트**: 30% 이상
- **리뷰 승인 조건**: 테스트 포함 필수

---

### 13. 온보딩 시간 (Onboarding Time)

#### 정의
신규 개발자가 TDD를 이해하고 적용하기까지의 시간

#### 측정 방법
- 온보딩 체크리스트 완료 시간
- 첫 TDD PR 제출까지의 시간

#### 목표 값
- **온보딩 기간**: 2주 이내
- **첫 TDD PR**: 1주 이내

---

## 📊 대시보드 예시

### 주간 대시보드
```
┌─────────────────────────────────────────────────────┐
│  📊 TDD Weekly Dashboard                           │
├─────────────────────────────────────────────────────┤
│  📈 Coverage                                        │
│     core/      : ████████████████░░░░ 82.5%        │
│     services/  : ████████████░░░░░░░░ 65.3%        │
│     handlers/  : ████████░░░░░░░░░░░░ 45.7%        │
│     Total      : ██████████████░░░░░░ 71.2%        │
├─────────────────────────────────────────────────────┤
│  🧪 Tests                                           │
│     Total: 67 (+12 this week)                      │
│     Passing: 65 (97%)                              │
│     Failing: 2                                     │
├─────────────────────────────────────────────────────┤
│  🐛 Bugs                                            │
│     Found by Tests: 8                              │
│     Production Bugs: 2                             │
│     Detection Rate: 80%                            │
├─────────────────────────────────────────────────────┤
│  🚀 Velocity                                        │
│     Features Delivered: 5                          │
│     Avg Debug Time: 2.5h (-30%)                    │
│     Refactoring Commits: 7                         │
├─────────────────────────────────────────────────────┤
│  👥 Team                                            │
│     TDD Adoption: 75%                              │
│     PRs with Tests: 15/20                          │
└─────────────────────────────────────────────────────┘
```

---

## 🎯 목표 트래킹

### 단기 목표 (2개월)
- [x] 전체 커버리지 70% 달성
- [x] core/ 모듈 80% 커버리지
- [x] 100+ 테스트 작성
- [x] TDD 채택률 50%
- [x] 버그 발견율 50%

### 중기 목표 (6개월)
- [ ] 전체 커버리지 80% 달성
- [ ] services/ 80% 커버리지
- [ ] 200+ 테스트 작성
- [ ] TDD 채택률 80%
- [ ] 버그 발견율 70%
- [ ] 디버깅 시간 50% 감소

### 장기 목표 (1년)
- [ ] 전체 커버리지 90% 달성
- [ ] 모든 모듈 80% 이상
- [ ] 300+ 테스트 작성
- [ ] TDD 채택률 100%
- [ ] 버그 발견율 90%
- [ ] 프로덕션 버그 90% 감소

---

## 📉 리스크 지표

### 위험 신호 (Red Flags)
- 🚨 **커버리지 하락**: 주간 5% 이상 감소
- 🚨 **테스트 실패**: 24시간 이상 실패 상태 유지
- 🚨 **TDD 채택률 저하**: 50% 미만
- 🚨 **프로덕션 버그 증가**: 전월 대비 50% 이상 증가

### 대응 조치
| 위험 | 조치 |
|------|------|
| 커버리지 하락 | 즉시 팀 미팅 소집 |
| 테스트 실패 | GitHub Issue 자동 생성 |
| 채택률 저하 | TDD 워크숍 재실시 |
| 버그 증가 | 코드 프리즈 및 품질 점검 |

---

## 🔄 측정 자동화

### 자동 수집 스크립트

**scripts/collect-metrics.js**:
```javascript
#!/usr/bin/env node
const fs = require('fs');
const { execSync } = require('child_process');

function collectMetrics() {
  const metrics = {
    timestamp: new Date().toISOString(),
    coverage: getCoverage(),
    testCount: getTestCount(),
    passRate: getPassRate(),
    velocity: getVelocity(),
    bugs: getBugMetrics()
  };

  // JSON으로 저장
  fs.writeFileSync(
    `metrics/metrics-${Date.now()}.json`,
    JSON.stringify(metrics, null, 2)
  );

  console.log('✅ Metrics collected successfully');
}

function getCoverage() {
  // coverage/coverage-summary.json 읽기
  const coverage = JSON.parse(
    fs.readFileSync('coverage/coverage-summary.json', 'utf8')
  );

  return {
    total: coverage.total.lines.pct,
    core: calculateModuleCoverage(coverage, 'src/core'),
    services: calculateModuleCoverage(coverage, 'src/services'),
    handlers: calculateModuleCoverage(coverage, 'src/handlers')
  };
}

function getTestCount() {
  const output = execSync('npm test -- --listTests', { encoding: 'utf8' });
  return output.split('\n').filter(Boolean).length;
}

function getPassRate() {
  // Jest 결과 파싱
  return 97.0; // 예시
}

function getVelocity() {
  // Git log 분석
  return {
    commits: 25,
    features: 5,
    refactorings: 7
  };
}

function getBugMetrics() {
  // GitHub Issues API 호출
  return {
    found: 8,
    fixed: 6,
    production: 2
  };
}

collectMetrics();
```

### Cron 설정
```yaml
# .github/workflows/collect-metrics.yml
name: Collect Metrics

on:
  schedule:
    - cron: '0 0 * * *'  # 매일 자정

jobs:
  metrics:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Collect Metrics
        run: |
          npm install
          npm run test:coverage
          node scripts/collect-metrics.js
      - name: Commit Metrics
        run: |
          git config user.name "Metrics Bot"
          git config user.email "bot@example.com"
          git add metrics/
          git commit -m "chore: collect daily metrics"
          git push
```

---

## 📚 참고 자료

- [Jest Coverage Report](https://jestjs.io/docs/cli#--coverageboolean)
- [Code Coverage Best Practices](https://martinfowler.com/bliki/TestCoverage.html)
- [Measuring TDD Success](https://www.thoughtworks.com/insights/blog/measuring-tdd)

---

**마지막 업데이트**: 2025-11-22
**검토 주기**: 주간
**책임자**: TDD Champion

---

## 🎯 PM 관리 지표

### PM 효율성 지표

#### 1. 의사결정 속도 (Decision Velocity)
- **정의**: PM이 이슈를 인지하고 결정하기까지의 시간
- **목표**: < 1시간 (긴급), < 4시간 (일반)
- **측정**: 이슈 발생 → 대응 완료 시간

#### 2. AI 팀 활용률 (AI Team Utilization)
- **정의**: AI 팀원들의 작업 활용 시간
- **목표**: Gemini 70%, Qwen 85%, Codex 60%
- **측정**: 실제 작업 시간 / 가용 시간

#### 3. 품질 게이트 통과율 (Quality Gate Pass Rate)
- **정의**: 첫 시도에 품질 게이트를 통과한 비율
- **목표**: 85% 이상
- **측정**: 통과 횟수 / 총 시도 횟수

#### 4. 계획 정확도 (Plan Accuracy)
- **정의**: 실제 소요 시간 vs 예상 시간
- **목표**: ±20% 이내
- **측정**: |실제 - 예상| / 예상 × 100

---

## 📊 PM 보고 자동화

### 일일 스탠드업 (자동)
```
Claude Code (PM) - Daily Report
Generated: 2025-11-22 09:00

Yesterday:
✅ Task 1.1 완료 (Jest 설정)
✅ Task 1.2 진행 중 (50%)

Today:
🎯 Task 1.2 완료 목표
🎯 Task 1.3 착수

Blockers:
🟢 없음

Risks:
🟡 Mocking 복잡도 - 모니터링 중
```

### 주간 리포트 (자동)
```
Claude Code (PM) - Weekly Report
Week: Nov 18-22, 2025

📊 Progress:
- Phase 1: 60% → 100% (완료!)
- Overall: 10% → 17% (+7%)

📈 Metrics:
- Coverage: 15% → 25% (+10%)
- Tests: 3 → 12 (+9)
- Pass Rate: 100%

🎖️ Achievements:
- Jest 인프라 구축
- CI/CD 통합 완료
- 기존 테스트 전환

⚠️ Risks:
- 없음

🎯 Next Week:
- Phase 2 착수
- core/ 모듈 테스트 시작
```

### 월간 리뷰 (수동 + 자동)
```
Claude Code (PM) - Monthly Review
Month: November 2025

🎯 Milestones:
✅ Milestone 1: Infrastructure (Week 2) - 완료
⏳ Milestone 2: Core Modules (Week 4) - 진행중

📊 Overall Progress: 25%

💰 Resource Usage:
- Gemini API: $12 (예산 내)
- Qwen Local: $0
- Codex API: $8 (예산 내)

📈 Trend Analysis:
- 커버리지 증가율: +10% weekly (양호)
- 테스트 추가율: +8 tests/week (목표 초과)
- TDD 채택: 75% → 85% (개선 중)

🏆 Top Contributors:
1. Qwen: 15 tests generated
2. Gemini: 8 BDD specs created
3. Codex: 5 refactoring suggestions
```

---

**마지막 업데이트**: 2025-11-22
**PM**: Claude Code (총괄 책임 PM)
**검토 주기**: 실시간 모니터링
**승인 권한**: Phase별 최종 승인
